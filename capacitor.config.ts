import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'aica.starter.io',
  appName: 'aica-starter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
